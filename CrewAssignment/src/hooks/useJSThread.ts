/**
 * Reports JS thread responsiveness (busy time / drift in ms).
 * TODO: implement with a setInterval drift measurement.
 */
export function useJSThread(): number {
  return 0;
}

export default useJSThread;
