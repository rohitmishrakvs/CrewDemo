/**
 * Measures rendered frames-per-second.
 * TODO: implement with requestAnimationFrame sampling over a rolling window.
 */
export function useFPS(): number {
  return 60;
}

export default useFPS;
